begin
    let int ctr = 0
    let int prev = 0
    let int curr = 1
    let int tmp = 0
    while (ctr < 19)
		println prev
        tmp = (curr + prev)
        prev = curr
        curr = tmp
        ctr = (ctr + 1)
    end
end